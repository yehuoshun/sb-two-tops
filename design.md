# 设计文档 — sb-two-tops

> 两个陀螺自动化脚本设计方案。

## 目标

1. **副本自动化** — 材料本、金币本等日常刷取全自动挂机
2. **连招自动化** — 战斗技能循环/DPS 优化（后续阶段，从社区宏指令蒸馏）

## 已知约束

- 环境：Windows 客户端，窗口化 1080p
- 开发环境：Linux（代码开发+逻辑验证），无 Windows 测试环境
- 满级账号，所有副本已解锁
- ACE 反作弊检测策略：**检测进程**，而非检测输入模拟
- **技术选型**：纯 Python，PyInstaller 编译为 sihost.exe 伪装系统进程

## 已确认信息

### 目标副本
- **探险** — 角色突破材料本（收集血清，打怪+摸储存点）
- **皎皎币/勘探** — 金币本（激活勘探器+保护）
- 游戏无体力/精力系统，可无限刷

### 战斗需求
- 日常阶段：**进本到点位，按 Q 大招即可**，不需要复杂连招
- 连招格式：文本宏指令（如 `eee, qaq`），后续从社区收集

### 副本流程
- 需要基于**当前页面状态**做判断，而非固定流程
- 结算界面有"继续挑战"按钮，可实现循环刷

### ACE 检测情况
- 仅国服有检测，检测方式：进程检测
- 已被检测项目：ok-dna、kk-dna、susu、魔改susu、维乐等
- 规避方案：编译为 sihost.exe，不暴露进程名特征

## 架构设计

### 状态机驱动

脚本核心是**状态机**，每轮截图 → 识别当前页面 → 执行对应操作：

```
┌──────────┐     ┌──────────────┐     ┌──────────┐
│  主城    │ ──→ │  副本菜单    │ ──→ │  确认进入 │
└──────────┘     └──────────────┘     └─────┬────┘
                                            │
                                      ┌─────▼────┐
                                      │  加载中   │
                                      └─────┬────┘
                                            │
                                      ┌─────▼────┐
                                      │  战斗中   │ ← 按 Q 大招
                                      └─────┬────┘
                                            │
                                      ┌─────▼──────┐
                                      │  结算界面   │ ← 有"继续挑战"按钮
                                      └──┬──────┬──┘
                              ┌──────────┘      └──────────┐
                         ┌────▼───┐                   ┌────▼───┐
                         │ 继续挑战│                   │ 返回主城│
                         └────┬───┘                   └────┬───┘
                              │                            │
                              ▼                            ▼
                        ┌──────────┐                 ┌──────────┐
                        │ 确认进入  │                 │  主城    │
                        └──────────┘                 └──────────┘
```

### 状态识别方式
- 每个状态对应一组 UI 特征图模板（OpenCV 模板匹配）
- 截图 → 与所有状态模板匹配 → 最高置信度即为当前状态
- 使用 `collect_templates.py` 在 Windows 上采集模板

## 代码结构

```
.
├── screenshot.py             # 旧截图测试工具
├── requirements.txt          # Python 依赖
├── config.yaml               # 配置（副本目标、连招、输入方式等）
├── agent.md                  # Agent 快速上手指南
├── design.md                 # 本文件
├── src/
│   ├── __init__.py
│   ├── capturer.py           # 截图模块（mss + win32 窗口查找）
│   ├── matcher.py            # 图像匹配模块（OpenCV 模板匹配）
│   ├── inputer.py            # 输入模拟模块（win32 SendInput + pyautogui 回退）
│   ├── state_machine.py      # 状态机引擎（状态流转+超时检测）
│   ├── states.py             # 各页面状态处理器（占位，需模板填充）
│   ├── collect_templates.py  # 模板采集工具（在 Windows 上运行）
│   └── main.py               # 入口
├── templates/                # UI 特征图模板（待采集）
└── .github/workflows/
```

## 开发阶段

### Phase 1：基础框架 ✅
- [x] 截图模块封装（mss 窗口截图）
- [x] 状态机引擎
- [x] 模板匹配工具
- [x] 输入模拟工具
- [x] 主入口

### Phase 2：模板采集 + 流程联调（当前阶段）
- [ ] 在 Windows 上运行 `collect_templates.py` 采集各页面截图
- [ ] 裁剪关键特征图（按钮、图标等）
- [ ] 填入 states.py 中的实际坐标/匹配逻辑
- [ ] 跑通 主城→选本→进本→战斗→结算→循环 全流程

### Phase 3：战斗优化
- [ ] 收集社区宏指令
- [ ] 实现高级连招/宏解析

### Phase 4：打包与部署
- [ ] PyInstaller 编译为 sihost.exe
- [ ] 使用文档