# 设计文档 — sb-two-tops

> 两个陀螺自动化脚本设计方案。

## 目标

1. **副本自动化** — 材料本、金币本等日常刷取全自动挂机
2. **连招自动化** — 战斗技能循环/DPS 优化（后续阶段，从社区代码蒸馏）

## 已知约束

- 环境：Windows 客户端，窗口化 1080p
- 无测试环境，代码需设计为可验证模式（单元测试 + 截图模拟）
- 满级账号，所有副本已解锁
- ACE 反作弊检测策略：**检测进程**，而非检测输入模拟

## 已确认信息

### 目标副本
- **探险** — 角色突破材料本（收集血清，打怪+摸储存点）
- **皎皎币** — 金币本（勘探/无尽，激活勘探器+保护）
- 游戏无体力/精力系统，可无限刷

### 战斗需求
- 日常阶段：**进本到点位，按 Q 大招即可**，不需要复杂连招
- 连招优化：后续阶段从社区代码蒸馏

### 副本流程
- 需要基于**当前页面状态**做判断，而非固定流程
- 结算界面有"继续挑战"按钮，可实现循环刷

### ACE 检测情况
- 仅国服有检测
- 检测方式：**进程检测**（不是输入检测）
- 已被检测的项目：ok-dna（开源）、kk-dna（闭源）、susu、魔改susu、维乐等
- 规避思路：进程名/特征不与已知项目撞车

## 架构设计

### 状态机驱动

脚本核心是**状态机**，每轮截图 → 识别当前页面 → 执行对应操作：

```
┌──────────┐     ┌──────────────┐     ┌──────────┐
│  主城    │ ──→ │  副本选择    │ ──→ │  确认进入 │
└──────────┘     └──────────────┘     └─────┬────┘
                                            │
                                      ┌─────▼────┐
                                      │  加载中   │
                                      └─────┬────┘
                                            │
                                      ┌─────▼────┐
                                      │  战斗中   │ ← 按 Q 大招
                                      └─────┬────┘
                                            │
                                      ┌─────▼──────┐
                                      │  结算界面   │ ← 有"继续挑战"按钮
                                      └──┬──────┬──┘
                              ┌──────────┘      └──────────┐
                         ┌────▼───┐                   ┌────▼───┐
                         │ 继续挑战│                   │ 返回主城│
                         └────┬───┘                   └────┬───┘
                              │                            │
                              ▼                            ▼
                        ┌──────────┐                 ┌──────────┐
                        │ 确认进入  │                 │  主城    │
                        └──────────┘                 └──────────┘
```

### 状态识别方式
- 每个状态对应一组 UI 特征图（模板匹配）
- 截图 → 与各状态模板匹配 → 置信度最高的状态即为当前状态
- 关键特征按钮：继续挑战、确认进入、加载界面、副本入口等

### 进程隐蔽方案（推荐：编译打包）
- 使用 PyInstaller 编译为单 exe
- 改名成 Windows 常见进程名（如 `runtimebroker.exe`、`taskhostw.exe`）
- 避免 `python.exe`、`py` 等明显进程名
- 避免使用已知被检测项目的代码结构和命名

## 开发阶段

### Phase 1：基础框架
- [ ] 截图模块封装（mss 窗口截图）
- [ ] 状态机引擎
- [ ] 模板匹配工具
- [ ] 输入模拟工具

### Phase 2：副本流程
- [ ] 各页面状态截图采集
- [ ] 模板训练（从实际截图裁剪特征图）
- [ ] 主城→选本→进本→战斗→结算 全流程
- [ ] 循环刷逻辑

### Phase 3：战斗优化
- [ ] 社区代码分析 + 蒸馏连招
- [ ] 高级战斗逻辑

### Phase 4：打包与部署
- [ ] PyInstaller 编译
- [ ] 进程名/特征混淆
- [ ] 使用文档

## 技术选型（待定）

| 模块 | 候选方案 | 说明 |
|------|---------|------|
| 截图 | mss | 窗口截图，性能好 |
| 图像识别 | OpenCV 模板匹配 | 静态 UI 识别，轻量高效 |
| 输入模拟 | pyautogui / win32 SendInput | ACE 不检测输入 |
| 状态管理 | 状态机 | 页面流转清晰 |
| 打包 | PyInstaller | 编译为单 exe 隐藏进程特征 |